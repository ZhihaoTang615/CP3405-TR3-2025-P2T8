pytest
numpy
pandas
scikit-learn
# SmartSeat – ML Baselines

This repo contains:
- A rule-based baseline (`smartseat_ml/baseline.py`)
- An open-source occupancy model adaptation (`smartseat_ml/open_source_model.py`)
- Minimal dataset: `external/Occupancy_model/datatraining.txt`

## Run locally

CI runs on GitHub Actions and prints metrics from tests.

